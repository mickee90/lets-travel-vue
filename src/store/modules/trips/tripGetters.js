export const getters = {
    getTrips(state) {
        return state.trips;
    },
    getTrip(state) {
        return state.trip;
    },
    getTripImages(state) {
        return state.tripImages;
    }
}